<div>
    <div class="card">
        <div class="card-header">
            <h3>عمال التدريب </h3>
        </div>
        <div class="card-body">
             <table class="table table-responsive" style="direction:rtl">
                <thead>
                    <tr>
                        <td>الكود</td>
                        <td>الاسم</td>
                        <td>القسم</td>
                        <td>عدد الايام</td>
                        <td>؟</td>
                    </tr>
                </thead>
                <tbody>
                    
                </tbody>
             </table> 
        </div>
    </div>
</div>
<script>
    let data=readSQL("select file_number,name,joining_date,job_title,days from ak_training()");
    console.log(data);
    if(data.length==0)
    {
        $("tbody").append(
            "<tr><td colspan='5'>"+
             "<h3 style='text-align:center'>لا توجد نتائج</h3>"
            +"</td></tr>"
        )
    }else
    {
        for(let i=0;i<data.length;i++)
        {
            $("tbody").append
            (
                "<tr><td>"+
                data[i].file_number
                +"</td><td>"+//
                data[i].name
                +"</td><td>"+//
                data[i].job_title
                +"</td><td>"+//
                data[i].days
                +"</td><td>"+//
                '<button onclick=open_modal("'+data[i].file_number+')">تغيير القسم</button>'
                +"</td></tr>"
            )
        }
        
    }
</script>
