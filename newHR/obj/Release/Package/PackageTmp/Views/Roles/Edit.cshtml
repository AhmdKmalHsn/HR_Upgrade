
@{
    ViewBag.Title = "Edit";
}
@using WebMatrix.Data;
@{
    var db = Database.Open("CS");
    var auths = "select Id,Name from accessibility";
    
}
<h2>تعديل الصلاحيات</h2>
<hr>
<form method="post" action="" dir="rtl">


    
    <div class="row">
        <div class="col-6">
            <div class="input-group">
                <label class="input-group-text"> اسم الصلاحية  </label>
                <input class="form-control" name="RoleName" />
            </div>
        </div>
        <div class="col-6">
            <div class="input-group">
                <label class="input-group-text">الاجازات  </label>
                <select id="" class="form-select">

                    @foreach (var row in db.Query(auths))
                    {
                        <option style="text-align:right" value="@row.id">@row.Name</option>
                    }
                </select>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-6">
            <div class="input-group">
                <label class="input-group-text">المأموريات </label>
                <select id="" class="form-select">

                    @foreach (var row in db.Query(auths))
                    {
                        <option style="text-align:right" value="@row.id">@row.Name</option>
                    }
                </select>
            </div>
        </div>
        <div class="col-6">
            <div class="input-group">
                <label class="input-group-text"> الحضور والانصراف</label>
                <select id="" class="form-select">

                    @foreach (var row in db.Query(auths))
                    {
                        <option style="text-align:right" value="@row.id">@row.Name</option>
                    }
                </select>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-6">
            <div class="input-group">
                <label class="input-group-text"> الأضافي</label>
                <select id="" class="form-select">

                    @foreach (var row in db.Query(auths))
                    {
                        <option style="text-align:right" value="@row.id">@row.Name</option>
                    }
                </select>
            </div>
        </div>
        <div class="col-6">
            <div class="input-group">
                <label class="input-group-text"> المرتب</label>
                <select id="" class="form-select">

                    @foreach (var row in db.Query(auths))
                    {
                        <option style="text-align:right" value="@row.id">@row.Name</option>
                    }
                </select>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-6">
            <div class="input-group">
                <label class="input-group-text">قفل الفترات</label>
                <select id="" class="form-select">

                    @foreach (var row in db.Query(auths))
                    {
                        <option style="text-align:right" value="@row.id">@row.Name</option>
                    }
                </select>
            </div>
        </div>
        <div class="col-6">
            <div class="input-group">
                <label class="input-group-text">ماكينة البصمة </label>
                <select id="" class="form-select">

                    @foreach (var row in db.Query(auths))
                    {
                        <option style="text-align:right" value="@row.id">@row.Name</option>
                    }
                </select>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-6">
            <div class="input-group">
                <label class="input-group-text"> تقرير الغياب</label>
                <select id="" class="form-select">

                    @foreach (var row in db.Query(auths))
                    {
                        <option style="text-align:right" value="@row.id">@row.Name</option>
                    }
                </select>
            </div>
        </div>
        <div class="col-6">
            <div class="input-group">
                <label class="input-group-text"> تقرير مفردات المرتب</label>
                <select id="" class="form-select">

                    @foreach (var row in db.Query(auths))
                    {
                        <option style="text-align:right" value="@row.id">@row.Name</option>
                    }
                </select>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-6">
            <div class="input-group">
                <label class="input-group-text"> الولوج</label>
                <select id="" class="form-select">

                    @foreach (var row in db.Query(auths))
                    {
                        <option style="text-align:right" value="@row.id">@row.Name</option>
                    }
                </select>
            </div>
        </div>
        <div class="col-6">
            <div class="input-group">
                <label class="input-group-text"> الورديات الاحتياطية</label>
                <select id="" class="form-select">

                    @foreach (var row in db.Query(auths))
                    {
                        <option style="text-align:right" value="@row.id">@row.Name</option>
                    }
                </select>
            </div>
        </div>
    </div>
    <hr>
            <div>
                <label> </label>
                <input type="submit" value="Save" class="btn btn-outline-success" />
            </div>

       
    
</form>
<script>

</script>





























